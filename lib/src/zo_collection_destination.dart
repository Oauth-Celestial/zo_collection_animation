import 'package:flutter/material.dart';

class ZoCollectionDestination extends StatelessWidget {
  final Widget child;

  const ZoCollectionDestination({required super.key, required this.child});

  @override
  Widget build(BuildContext context) {
    return child;
  }
}
